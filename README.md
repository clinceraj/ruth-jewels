# RUTH-JEWELS

HANDPICKED INDIAN CLASSICAL JEWELERIES SHOP

## Setup Instructions

To set up this repository locally:

```bash
git clone https://github.com/clinceraj/ruth-jewels.git
cd ruth-jewels
```

## Repository Initialization (If starting fresh)

```bash
git init
git add .
git commit -m "Initial commit - Ruth Jewels"
git remote add origin https://github.com/clinceraj/ruth-jewels.git
git push -u origin main
```
